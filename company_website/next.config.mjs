/** @type {import('next').NextConfig} */
const nextConfig = {
  //   images: {
  //     domains: ["firebase.google.com"],
  //   },
  images: {
    domains: [
      "upload.wikimedia.org",
      "www.appsrhino.com",
      "firebase.google.com",
      "flutter.dev",
    ],
  },
};

export default nextConfig;

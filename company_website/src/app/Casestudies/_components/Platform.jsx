import React from "react";

const Platform = ({ text }) => {
  return (
    <span className="px-4 py-2 border  font-jakarta  border-black  text-black  rounded-full text-sm font-medium">
      {text}
    </span>
  );
};

export default Platform;
